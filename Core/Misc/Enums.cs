namespace GameBalance
{
    public enum ResultType
    {
        Unknown,
        Successful,
        Failed,
        Administrator,
        Cancelled
    }
    public enum DebugMode
    {
        Disabled,
        Enabled
    }
    public enum ShellType
    {
        Cmd,
        PowerShell,
    }
}
