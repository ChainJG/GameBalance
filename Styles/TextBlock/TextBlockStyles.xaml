<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BaseTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FontSize" Value="{StaticResource DefaultFontSize}" />
        <Setter Property="FontFamily" Value="{StaticResource DefaultGameBalanceFontFamily}" />
    </Style>

    <Style
        x:Key="{x:Type TextBlock}"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="{x:Type TextBlock}" />

    <!--#region Title / Subtitle Text Style-->

    <Style
        x:Key="TitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource TitleFontSize}" />
    </Style>

    <Style
        x:Key="SubTitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource SubTitleFontSize}" />
    </Style>

    <!--#endregion-->

    <!--#region Button Text Style-->

    <Style
        x:Key="ButtonTitleStyle"
        BasedOn="{StaticResource BaseTextStyle}"
        TargetType="TextBlock">
        <Setter Property="FontSize" Value="{StaticResource ButtonFontSize}" />
        <Setter Property="FontFamily" Value="{StaticResource ButtonFontFamily}" />
    </Style>

    <!--#endregion-->

</ResourceDictionary>