<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:GameBalance.Framework.Controls"
    xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">

    <Style BasedOn="{StaticResource {x:Type controls:InteractiveCard}}" TargetType="{x:Type controls:DockItemControl}">
        <Setter Property="NormalBackground" Value="Transparent" />
        <Setter Property="IsSelected" Value="{Binding IsSelected}" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DockItemControl}">
                    <Border
                        Width="60"
                        Height="60"
                        Background="{TemplateBinding CurrentBackground}"
                        BorderBrush="{TemplateBinding CurrentBorder}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">

                        <StackPanel
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsHitTestVisible="False"
                            Orientation="Vertical">

                            <materialDesign:PackIcon
                                Width="20"
                                Height="20"
                                Margin="3"
                                Foreground="{StaticResource IconBrush}"
                                Kind="{Binding Icon}" />

                            <TextBlock Style="{StaticResource ButtonTitleStyle}" Text="{Binding Name}" />

                        </StackPanel>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>