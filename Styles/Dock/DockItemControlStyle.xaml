<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:GameBalance.Framework.Controls">

    <Style TargetType="{x:Type controls:DockControl}">

        <Setter Property="ItemsPanel">

            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>


        <Setter Property="ItemTemplate">

            <Setter.Value>
                <DataTemplate>
                    <controls:DockItemControl
                        Margin="{StaticResource DockItemSpacing}"
                        Command="{Binding Command}"
                        CommandParameter="{Binding CommandParameter}"
                        SelectionManager="{Binding RelativeSource={RelativeSource AncestorType=controls:DockControl}, Path=SelectionManager}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>


        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DockControl}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>